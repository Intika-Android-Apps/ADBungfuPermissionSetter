<shortcuts xmlns:android="http://schemas.android.com/apk/res/android">
    <shortcut
        android:shortcutId="full"
        android:enabled="true"
        android:icon="@mipmap/ic_launcher_adb"
        android:shortcutShortLabel="@string/s1"
        android:shortcutLongLabel="@string/s1"
        android:shortcutDisabledMessage="@string/s1">
        <intent
            android:action="android.intent.action.VIEW"
            android:targetPackage="com.oF2pks.adbungfu"
            android:targetClass="com.pavelsikun.runinbackgroundpermissionsetter.MainActivity">
        </intent>
        <!-- If your shortcut is associated with multiple intents, include them
             here. The last intent in the list determines what the user sees when
             they launch this shortcut. -->
        <categories android:name="android.shortcut.conversation" />
    </shortcut>
    <shortcut
        android:shortcutId="RUN_IN_BACKGROUND"
        android:enabled="true"
        android:icon="@mipmap/ic_launcher"
        android:shortcutShortLabel="@string/s2"
        android:shortcutLongLabel="@string/s2"
        android:shortcutDisabledMessage="@string/s2">
        <intent
            android:action="android.intent.action.VIEW"
            android:targetPackage="com.oF2pks.adbungfu"
            android:targetClass="com.pavelsikun.runinbackgroundpermissionsetter.MainActivity">
            <extra android:name="extraID" android:value="RUN_IN_BACKGROUND" />
        </intent>
        <!-- If your shortcut is associated with multiple intents, include them
             here. The last intent in the list determines what the user sees when
             they launch this shortcut. -->
        <categories android:name="android.shortcut.conversation" />
    </shortcut>
    <!-- Specify more shortcuts here. -->
</shortcuts>
